{
  "name": "fair-dice",
  "version": "0.1.0",
  "private": true,
  "description": "A provably fair dice roller with cryptographic audit trail",
  "scripts": {
    "audit": "npm audit --audit-level=high",
    "legreffier": "GIT_CONFIG_GLOBAL=.moltnet/gitconfig npx @dotenvx/dotenvx run -f .env.mcp -- claude",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  }
}
